<h3>Article Index</h3> 

<table>
    <thead>
        <tr>
            <th>Title</th>
        
        
            <th>Description</th>
        
            <th colspan = '3'>Action</th>


            
        </tr>
    </thead>


    <tbody>
        <% @articles.each do |article| %>
            <tr>
                <td> <%= article.title %> </td>
                <td> <%= article.description %></td>
                
                <td> <%= link_to 'Show', article_path(article) %></td>
                <td> <%= link_to 'Edit', edit_article_path(article.id), data: { turbo_method: :edit } %></td>
                <td><%= link_to 'Delete', article_path(article.id), data: { turbo_method: :delete, turbo_confirm: "Are you sure?"} %></td>
                
            </tr>
        <%end%>
    </tbody>
   
</table>
<br>
<td><%= link_to 'Create A New Article', new_article_path %></td> ||
<%= link_to 'View Home Page', root_path %> ||
<%= link_to 'View About Page', about_path %> ||
